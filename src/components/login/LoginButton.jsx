import React from "react";

const LoginButton = () => {
  return (
    <button className="block mx-auto w-full p-3 rounded-full bg-blue ">
      Đăng nhập
    </button>
  );
};

export default LoginButton;
